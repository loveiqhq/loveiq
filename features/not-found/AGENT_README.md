# features/not-found

**Purpose:** 404 page UI rendered by `app/not-found.tsx`.

**Entry:** `ui/NotFoundPage.tsx`.

**Belongs:** 404 layout/copy. Reuses `@features/landing/ui/NavSection` + `FooterSection`.
