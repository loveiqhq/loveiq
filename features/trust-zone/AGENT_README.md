# features/trust-zone

**Purpose:** `/trust-zone` page — privacy/security trust signals.

**Entry:** `ui/TrustZonePage.tsx`. Has its own layout at `app/trust-zone/layout.tsx`.

**Belongs:** trust-zone UI.

**Does NOT belong:** general legal copy (→ `features/legal/`).
