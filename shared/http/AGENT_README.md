# shared/http

CSRF (`csrf.ts` server, `csrf-client.ts` client), rate limiting (`ratelimit.ts` Upstash+memory fallback), fetch wrappers (`fetch-with-timeout.ts`), circuit breaker (`circuit-breaker.ts`), after-response scheduler (`after-response.ts`). Used by every API route.
