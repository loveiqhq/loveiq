## Summary

<!-- Brief description of the changes (1-3 sentences) -->

## Changes

-

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `npm run build` succeeds
- [ ] `npm run check:migrations` passes (if `supabase/migrations/**` touched)
- [ ] Tested manually in browser (if UI change)
- [ ] No secrets committed (check `.env.example` if adding env vars)
- [ ] Security controls preserved (CSP, CSRF, rate limiting)
- [ ] Documentation updated
- [ ] No doc impact
- [ ] ADR added to `docs/adr/` (if architectural decision — scoring engine, CSP, migration strategy, auth model, payment flow)

## Screenshots

<!-- If UI change, add before/after screenshots -->
