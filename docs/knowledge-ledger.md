# Knowledge Ledger

> Owner: CODEOWNERS default
> Last verified: 2026-04-05
> Verified against: `app/api/**`, `package.json`, `.env.example`, `.github/workflows/**`, `.github/pull_request_template.md`

Project facts should not live only in repo memory or agent context. Record verified documentation changes here with the evidence path and the doc that now stores the fact.

| Date       | What changed or was learned                                                                                                             | Evidence                                                                                                                                                                     | Doc updated                                             | Why it matters                                                                                       |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 2026-04-05 | Public and admin API references were split and aligned to the actual `app/api/**/route.ts` inventory.                                   | `app/api/**/route.ts`, `lib/admin/roles.ts`                                                                                                                                  | `docs/api.md`, `docs/admin-api.md`                      | Prevents undocumented admin routes and stale method lists.                                           |
| 2026-04-05 | Toolchain versions were consolidated into one canonical version file sourced from `package.json` and CI.                                | `package.json`, `.github/workflows/ci.yml`                                                                                                                                   | `docs/versions.md`, `README.md`, `DEVELOPMENT.md`       | Stops version drift across onboarding and contributor docs.                                          |
| 2026-04-05 | Documentation truth checks became source-driven and CI-backed instead of `CLAUDE.md`-only.                                              | `scripts/check-docs-truth.mjs`, `.github/workflows/docs-truth.yml`                                                                                                           | `README.md`, `CONTRIBUTING.md`, `.github/README.md`     | Keeps docs coupled to routes, scripts, env vars, and pinned versions.                                |
| 2026-04-05 | Docs impact checks became blocking for high-risk changes unless the PR explicitly marks `No doc impact`.                                | `scripts/check-docs-impact.sh`, `.github/workflows/ci.yml`, `.github/pull_request_template.md`                                                                               | `CONTRIBUTING.md`, `.github/README.md`                  | Makes doc updates part of the review contract.                                                       |
| 2026-04-05 | Root onboarding now treats `npm run setup`, `npm run check`, and `npm run docs:truth` as the canonical local workflow.                  | `package.json`, `.env.example`, `.github/workflows/ci.yml`                                                                                                                   | `README.md`, `DEVELOPMENT.md`, `CONTRIBUTING.md`        | Reduces setup ambiguity and mismatched validation instructions.                                      |
| 2026-04-05 | The survey flow is now documented as a product/runtime surface, including restore order, storage keys, autosave, and retry behavior.    | `app/survey/page.tsx`, `components/survey/**`, `app/api/survey/route.ts`, `app/api/survey-partial/route.ts`, `app/api/survey-tracking/route.ts`                              | `docs/survey.md`, `README.md`                           | Prevents survey UX changes from being documented only through API contracts or component spelunking. |
| 2026-04-05 | Admin lookup is now domain-routed through local `AGENT_README.md` files and `docs/admin/domains/*.md` instead of a single global index. | `app/admin/AGENT_README.md`, `app/api/admin/AGENT_README.md`, `components/admin/AGENT_README.md`, `lib/admin/AGENT_README.md`, `docs/admin/domains/*.md`                     | `FILE_INDEX.md`, `docs/doc-inventory.md`, `README.md`   | Reduces admin search depth and makes cross-root discovery deterministic.                             |
| 2026-04-05 | The admin landing surface is `CommandCenterDashboard` on `/admin`, while `AdminStatsDashboard` remains a separate stats component.      | `app/admin/page.tsx`, `app/admin/layout.tsx`, `components/admin/CommandCenterDashboard.tsx`, `components/admin/AdminStatsDashboard.tsx`, `components/admin/AdminSidebar.tsx` | `docs/admin-dashboard.md`, `FILE_INDEX.md`, `README.md` | Removes ambiguity when editing the admin shell, landing page, or dashboard-related docs.             |
