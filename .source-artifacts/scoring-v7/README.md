Versioned source artifacts for the V7 scoring sync.

Files in this folder are the human-authored workbook and notes used to derive the scoring configuration changes implemented in code. They are kept out of the repo root to avoid clutter and are not read by the app at runtime.
